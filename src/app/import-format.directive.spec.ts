import { ImportFormatDirective } from './import-format.directive';

describe('ImportFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ImportFormatDirective();
    expect(directive).toBeTruthy();
  });
});
